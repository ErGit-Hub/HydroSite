/**
 * Плашка «Сайт в разработке» над шапкой. Когда сайт будет готов — false и выкладка.
 * Высота плашки задаётся в styles.scss (--dev-banner-h) и учитывается во всех
 * отступах под закреплённую шапку, поэтому выключать только здесь.
 */
export const SHOW_DEV_BANNER = true;
